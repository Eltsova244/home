from dotenv import load_dotenv
from flask
import os
load_dotenv()
print(os.getenv("HOGWARTS_URL"))
ab=SQLAchemy()
app= Flask(_name_)
config["SQLACEMY_DATABASE_URL"] = os.

db.init_app(app)
class Student(ab.Model)
{
    id=ab.Column
}